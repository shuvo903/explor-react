export default function Users (fetchUsers){

    // const users = use (fetchUsers);
    // console.log(users);

    return(
        <div className="card">
            <h3>Users:</h3>
        </div>
    )
}